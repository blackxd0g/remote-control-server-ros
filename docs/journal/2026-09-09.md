# 2026-09-09

## REL-PUB-01 — подготовка разрешённого выпуска
- Запрос: опубликовать сборку в Docker Hub/GitHub и исправить ошибки GitHub. Цель внесена в карту, разрешение зафиксировано DEC-007. Исходники сервера — основной каталог; публикационный checkout чистый. Relay содержит прежние локальные изменения, они сохранены.
- GitHub: последние verify сервера завершились failure; у отдельного приватного relay запусков Actions нет. Docker Desktop работает; стабильный GitHub release — v2.2.1. Проверяем причины failure, готовим предварительный amd64 выпуск с сохранением latest и без production deployment.

## REL-PUB-01 — причина CI и состав кандидата
- Последний code job падает на Windows-only Rust toolchain в Linux runner. Workflow задаёт Linux 1.98.0/toolchain и target, устанавливает musl-tools, собирает Web до Go embed и HBBR для Go binary integration tests. Контейнерная матрица amd64 согласно DEC-006; архитектурная поддержка в Dockerfile сохранена.
- Версия кандидата 2.3.0-rc.1, ограничения и порядок резервирования/отката описаны в release notes. All-in-one проходит локальную Docker-сборку с Web/Go/Rust проверками.
- Проверка состава выявила приватную адресацию в исторической Markdown-документации. Исходная история сохраняется локально; публикационная копия будет обезличена, значения в журнал не переносятся. Тестовые сетевые fixtures в Go проверяются отдельно от документации.

## REL-PUB-01 — проверки кандидата и блок публикации
- All-in-one 2.3.0-rc.1 amd64 собран. Web typecheck/build, Go fmt/tests/vet/static build, Rust fmt/clippy/38 tests/musl release PASS. Первоначальное нарушение gofmt в sqlstore/relay_operations.go исправлено форматированием. Попытка локального pnpm на Windows остановлена из-за TLS trust; Web успешно собран в Docker без отключения TLS. Удалённые CI ещё не запускались на новом diff.
- Реальный HBBR именно из кандидата: TestRelayBinaryWSS, TestRelayBinaryDurableRestart, TestRelayBinaryRotationQuarantine PASS (11.014s). Load/PostgreSQL/ARM64 в этом выпускном блоке не повторялись; их прежние результаты исторические.
- Контейнеры amd64 UID 65532: сервер healthz и три процесса PASS. Отдельная синтетическая 2.2.1 остановлена, том скопирован в новый; 2.3.0-rc.1 на копии healthy, приватная пара и JWT secret побайтно сохранились (значения не выводились). Прежний образ на сохранённом исходном томе снова healthy: проверен откат через резервную копию, а не downgrade изменённой БД.
- Relay-кандидат содержит HBBR, CA и outbox; shell/API/HBBS отсутствуют. Без credential-файла при заданном WSS URL завершается с ожидаемой ошибкой. Добавлены Dockerfile.rc с pinned source digest, compose.rc.yaml, release notes и новый verify workflow для отдельного репозитория.
- Подготовлены отдельные архивы Linux amd64 бинарников и SHA256SUMS в work/release-2.3.0-rc.1. Runtime-тома, rootfs-диагностика и credentials в эти архивы не включены.
- Automatic approval review заблокировал команду commit/push полного staged diff: потребовал trusted подтверждение конкретного репозитория и полного состава публикации из-за риска чувствительной внутренней информации. Commit/push этой команды не выполнены. Отправлен конкретный запрос на два GitHub-репозитория, два Docker Hub образа и перечисленные изменения. Ответ пока не получен; публикации и GitHub releases не выполнялись.
- Исходная история сохранена локально; в публикационной копии шести Markdown-файлов приватная адресация заменена документальным примером. Проверка маркеров токенов/приватных ключей и исключение runtime-файлов пройдены; git diff --check очищен. Stable/latest, production и видимость relay не менялись.

## REL-PUB-01 — конкретное подтверждение публикации
- Пользователь подтвердил перечисленные GitHub/Docker Hub репозитории и полный состав 2.3.0-rc.1. Продолжаем commit/push, проверку нового CI, публикацию двух amd64 rc-образов и GitHub prereleases. Stable/latest, production и private relay сохраняются.
