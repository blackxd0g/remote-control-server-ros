# 2026-09-07

## DOC-01 — проектная память и правила работы

- Запрос и план: настроить Markdown-журнал, компактную карту прошлых и будущих целей и AGENTS.md. План внесён в карту до реорганизации.
- Изменения: прежняя карта скопирована в [архив](../archive/roadmap-2026-09-07-before-docs-reorganization.md); расширены корневые правила, добавлены инструкции обоих проектов, [индекс](../README.md), [состояние](../project-state.md), [решения](../decisions.md) и шаблон журнала. Карта сокращается до целей, статусов и критериев.
- Проверки: SHA-256 исходной карты и архивной копии совпали до замены карты. Проверка ссылок и ID целей ожидается после правок. Код и runtime-тесты в этом блоке не менялись и не запускались.
- Результат: работа локальная; GitHub, Docker Hub и production не изменялись. История сохранена без выдачи старых результатов за новые проверки.
- Дальше: проверить документацию и закрыть DOC-01 по результатам.

### Завершение DOC-01 — 2026-09-07

- Карта сокращена до 56 строк; прежний текст сохранён отдельно. Все исходные ID PAR/REL/DOC присутствуют в новой карте, исторические этапы сведены в таблицу с оговорками об объёме приёмки.
- Проверка 26 локальных Markdown-ссылок новых документов прошла: отсутствующих файлов нет. Архив исключён из проверки ссылок как неизменённый снимок со старой базой путей; это пояснено в индексе.
- SHA-256 архива: `6D918B647E91F999F57D78BC9A10BC5D29FAF3BBF9E995B80D3024448EDCF90A`; совпадение с исходником проверено перед заменой.
- Первая попытка применения пакета правок была отклонена валидатором из-за двух операций над одним AGENTS.md; пакет исправлен и успешно применён. Потери файлов не было.
- DOC-01 выполнен. Следующие рабочие блоки вести по шаблону журнала; новые цели добавлять в карту до реализации. Публикаций и изменений production не выполнялось.
- Финальное уточнение после переноса DOC-01 в выполненные: карта содержит 55 строк; повторная проверка всех 27 локальных ссылок прошла.

## DOC-02 — инструкции разработки и тестирования

- Запрос и план: по подтверждению пользователя подготовить CONTRIBUTING.md и docs/testing.md для продолжения в новом чате; цель внесена в карту до реализации.
- Изменения: добавлены инструкции окружения, PowerShell-запуска UI/API с отдельными локальными данными, локальной контейнерной сборки и проверок Go/Rust/Vue/REL-02. Обновлены индекс, карта и состояние. Код приложения не менялся.
- Проверки: 25 локальных ссылок в двух новых документах и индексе прошли проверку существования; скрипты package.json найдены. Команды, версии, env aliases, пути, имена тестов и условия SKIP сверены с Dockerfile, Cargo.toml, go.mod, Vite/config, CI и тестами. Сборки и runtime-тесты в этой задаче не запускались.
- Находки: TestRelayBinaryWSS пропускается без RDS_TEST_HBBR_BINARY; CI его автоматически не активирует. Штатный all-in-one Compose указывает 2.1.1. Ограничения описаны без изменения конфигурации поставки. RDS_* имеют приоритет перед ART_*.
- Результат: DOC-02 выполнен; инструкции описывают воспроизводимые команды и не выдают статическую сверку за новый успешный runtime-прогон. Публикаций и production-изменений нет.
- Дальше: продолжать REL-02 по открытым критериям карты; подробные результаты следующих проверок добавлять отдельными блоками журнала.

## REL-02 — устойчивость управляющего канала, крупный локальный блок

- Запрос и план: пользователь поручил крупное обновление. До реализации в карте зафиксирован блок reconnect backoff/jitter, повторов команд, lifecycle ack/dedup в пределах control-сессии, таймаутов и проверок сбоев. Durable delivery через restart и multi-API этим блоком не закрываются.
- Изменения в работе: gateway повторяет команду с прежним ID/deadline, прекращает ожидание при disconnect, проверяет ready и ограничивает время обработки; lifecycle подтверждается после успешной записи и повтор события не вызывает повторный report. HBBR получил очередь подтверждений и ограниченный exponential backoff с jitter.
- Промежуточные проверки: Go gateway/API/store tests прошли в Linux Docker. Нативный Go был остановлен ошибкой доступа к кешу; Docker-проверки выполняются через разрешённый локальный engine. Rust-окружение потребовало правильного registry mount и явных Linux toolchain/target вместо настроек Windows; эти ошибки не являются успешными проверками.
- Публикаций и production-изменений не выполняется. После сборки предстоит тест настоящего HBBR и итоговая проверка.

### Результат блока устойчивости REL-02

- Изменены `art-api/internal/relaygateway/gateway.go`, добавлен `gateway_test.go`, обновлён `art-hbbr/src/control.rs`. Новых библиотек не потребовалось. Контракт, состояние, карта и DEVELOPMENT.md отдельного relay обновлены.
- Полный Go `go test -count=1 ./...`, `go vet ./...` и статическая Linux amd64 сборка API прошли. После добавления ещё двух сценариев повторно прошли все 6 gateway tests: retry/disconnect, lifecycle dedup/conflict, storage failure, report before ready, missing ready/reconnect, missing ack timeout.
- Rust fmt/clippy с `-D warnings`, locked offline workspace tests прошли: 31 unit test, включая 3 новых проверки backoff, очереди и ack. Замечание clippy о вложенном if исправлено до финального успешного прогона. Новый amd64 HBBR собран в `work/rel02/art-hbbr`.
- `TestRelayBinaryWSS` с новым бинарником прошёл без SKIP: проверены TLS trust, TCP/WS/mixed, permit, terminate/ack и поведение действующей пары после revoke. Диагностическая ошибка TLS в отрицательной части теста ожидаема.
- Локальный `remote-control-relay:rel02-dev` пересобран и запущен с network=none, UID 65532, read-only, cap-drop ALL и no-new-privileges. Running=true; временные контейнер и credential удалены. Это debug amd64, не версионный release; ARM64 и официальный клиент в этом блоке не проверялись. UI не менялся, повторная сборка не выполнялась.
- Ограничение: ack/dedup и очередь событий действуют внутри control-сессии. При смене эпохи/restart неподтверждённые события не восстанавливаются; при переполнении producer возможна потеря. Устойчивая доставка через restart, нагрузочная приёмка и multi-API остаются следующими задачами REL-02.
- Блок выполнен локально; REL-02 целиком не закрыт. GitHub/Docker Hub публикаций и обновления MikroTik не было.

## REL-02 — постоянная доставка событий

- План до реализации записан в карту: очередь на отдельном томе, постоянная авторизация UUID на API, транзакционные receipt/projection и восстановление после restart. Только локальная разработка.
- Реализация в работе: JSON snapshot с atomic rename/fsync, блокировка единственного владельца, лимит 4096 событий с резервом для закрытия активных пар и ограничение файла 2 MiB. Новая пара начинает передачу после сохранения active; startup формирует closed для прерванных пар.
- API сохраняет UUID/relay/generation до отправки permit. Receipt и проекция соединения обновляются одной транзакцией; повтор после reopen БД не повторяет изменение. Старое поколение ключа и чужой relay не принимаются. Новая control hello объявляет поддержку durable delivery.
- Добавлен отдельный записываемый named volume в dev-поставку relay; root filesystem остаётся read-only. Проверки Rust/Go запущены; результаты будут добавлены после завершения, не считаются пройденными заранее.

### Завершение локального блока durable delivery

- Изменены `art-hbbr/src/outbox.rs`, `telemetry.rs`, `control.rs`, `main.rs` и зависимость на уже используемый workspace UUID; API получил `relay_delivery.go`, миграции authorizations/receipts, подключение callbacks gateway и транзакционные проверки. В relay обновлены Dockerfile.dev/compose.dev.yaml/DEVELOPMENT.md. Контракт вынесен в [relay-durable-delivery.md](../relay-durable-delivery.md).
- Полный Go `go test -count=1 ./...` и `go vet ./...` прошли с включёнными обоими тестами настоящего HBBR; API статически собран для Linux amd64. Начальный store-тест обнаружил ошибку типа connection_type в INSERT; исправлено на числовое значение до успешного полного прогона.
- Rust fmt/clippy с `-D warnings`, workspace tests прошли: 33 unit tests. Четыре outbox-теста проверяют restart/стабильный ID/closed, испорченный файл/второго владельца, отказ записи/восстановление и резерв ёмкости. После уточнения запрета permits при заполнении повторены clippy и 10 HBBR tests; бинарник пересобран.
- Оба финальных binary-теста прошли без SKIP: прежний TLS/TCP/WS/mixed/permit/terminate/revoke и новый restart настоящего HBBR с недоступным API, заменой gateway, доставкой snapshot и последующей передачей данных. Reopen SQLite и 8 конкурентных повторов проверены отдельным store-тестом; PostgreSQL runtime не проверялся.
- Dev Compose config прошёл. Локальный образ с отдельным томом запущен и перезапущен: UID 65532, root read-only, credential mount read-only, outbox mount read-write, Running=true и snapshot version=1 после restart. Временные контейнер, анонимный тестовый том и файлы удалены. После финального изменения проверки ёмкости dev-образ пересобирается тем же Dockerfile; layout тома не менялся.
- Режим локальный, VERSION/release-теги не менялись, GitHub/Docker Hub/MikroTik не обновлялись. UI не менялся и повторно не собирался. Ограничения: нагрузка, retention/разбор очереди при ротации, PostgreSQL, multi-API, ARM64 и официальный клиент ещё требуют приёмки. Потеря самого тома протоколом не восстанавливается.
- Финальная пересборка `remote-control-relay:rel02-dev` завершена успешно; 48 локальных Markdown-ссылок и форматирование изменённых Go-файлов проверены. Локальный блок durable delivery выполнен; REL-02 целиком остаётся в работе.

## DASH-01 — просмотр онлайн-пользователей

- Запрос: dashboard показывает число пользователей онлайн, но не позволяет узнать кто это. Цель внесена в карту до реализации.
- Проверено по исходникам: `/api/admin/presence` уже возвращает список с именем, логином, статусом и last_seen_at; endpoint защищён users.read. Online означает активность неотозванной, неистёкшей сессии одобренного включённого пользователя за последние 3 минуты. Серверная логика и права не менялись.
- Изменения: в `DashboardView.vue` добавлены кнопка «Кто онлайн», native dialog с закрытием/Escape, поиск по имени/логину/устройству, таблица последней активности и устройства, обновление вручную и каждые 15 секунд. Список и счётчик используют один snapshot; при ошибке прежние данные очищаются. `MetricCard.vue` получил дополнительный slot для действия. Светлая/тёмная палитра берётся из текущих CSS variables.
- Проверки: vue-tsc --noEmit и Vite production build прошли. Запуск через pnpm сначала попытался восстановить зависимости и завершился ошибкой проверки TLS registry; проверка выполнена напрямую установленными инструментами без отключения TLS. Vite потребовал разрешённый доступ к родительским каталогам Windows. Промежуточная команда редактирования указала неверный относительный путь и не изменила файл; исправленный путь применён перед успешной сборкой.
- Browser smoke в этой задаче не выполнялся; поведение backend не менялось, Go/Rust тесты не повторялись. Сборка UI создана локально; API-контейнеры, Docker Hub/GitHub и production не обновлялись.
- Результат: DASH-01 выполнен локально. Открытые цели REL/PAR остаются в карте.

## REL-OPS-01 — эксплуатация очереди relay

- По запросу на крупное обновление до реализации внесён план диагностики, постоянного карантина после ротации и безопасного уплотнения истории. Действует запрет публикаций и production deployment.
- Реализация: новая панель очереди/хранилища/блокировки/карантина в RelayServersView; read-only delivery API и relays.write операция уплотнения до 1000 closed-записей старше 30 дней. Компактный архив сохраняет владельца UUID, поколение и ID событий; поздний повтор не создаёт проекцию заново и UUID нельзя выдать повторно.
- На relay snapshot v1 мигрирует в v2 с quarantine и временем создания событий. Только подтверждённый ответ credential_rotated переносит текущую запись в постоянный карантин; новые события продолжают доставку. Карантин входит в общий лимит 4096 и автоматически не удаляется. Диагностика содержит счётчики и первые 5 записей, без credentials.
- Промежуточно прошли Go store/gateway/API tests, Rust workspace tests (35), Vue typecheck/build и настоящий TestRelayBinaryRotationQuarantine: старые события не применены, карантин отображён через API, новые данные передаются. Полный итоговый прогон и пакетная проверка ещё выполняются.

### REL-OPS-01 — итог локальной приёмки

- Полный `go test -count=1 ./...` с заданным RDS_TEST_HBBR_BINARY (все три binary-сценария включены), `go vet ./...` и статическая сборка API завершились успешно. Итог повторно подтверждён после потери вывода завершившейся инструментальной сессии.
- Rust: 35 unit tests, fmt и clippy с запретом warnings прошли; HBBR собран Linux amd64. Vue typecheck и Vite build прошли. Проверка 1001 записи подтвердила размер пакета 1000, затем 1; это не нагрузочный benchmark.
- Образ `remote-control-relay:rel02-dev` пересобран локально. Smoke: сеть отключена, UID 65532, read-only root, отдельный writable том; запуск и restart успешны, snapshot версии 2 сохранён. Созданный smoke-контейнер, его одноразовый том и тестовые fixtures удалены.
- Обновлены relay-operations.md, durable-контракт, DEVELOPMENT.md relay, индекс, testing, состояние и карта. REL-OPS-01 завершён в указанном локальном объёме. Полный карантин остаётся на диске (в UI первые пять событий), компактный архив бессрочный, неиспользованные разрешения не очищаются автоматически.
- Не выполнены браузерная визуальная проверка, PostgreSQL runtime, ARM64, официальный клиент, публичный стенд и нагрузочная приёмка. REL-02 в целом остаётся открытой. GitHub/Docker Hub и production не изменялись; опубликованная версия остаётся 2.2.1.
- Финальная проверка документации: 50 локальных ссылок разрешаются. Git status relay выполнен с разовым safe.directory из-за отдельного sandbox-пользователя; глобальная конфигурация не менялась.

## REL-OPS-02 — начало
- Пользователь подтвердил надёжность relay и обслуживание карантина. Цель занесена в карту до реализации. План: постраничный WSS-доступ, экспорт авторизованному администратору, очистка по ревизии с резервной копией на relay до удаления и обязательным аудитом намерения; проверки отказов, нагрузки и PostgreSQL на одноразовом локальном стенде. Production-данные не затрагиваются.

### REL-OPS-02 — промежуточные результаты
- Реализованы WSS-страницы по 10 событий с ревизией, admin export и архивирование по точной ревизии. До очистки relay сохраняет эксклюзивный файл с fsync, API требует подтверждение и успешную запись намерения в аудит. Повтор последней операции после restart не удаляет новые события. Резервные файлы автоматически не удаляются.
- 37 Rust unit tests прошли, включая повреждённую резервную копию, stale revision, restart/retry и полный карантин 4096. Vue typecheck прошёл.
- PostgreSQL runtime: миграции, reopen, конкурентный replay, изоляция, ротация и уплотнение прошли. Измеренная нагрузка на синтетических данных: 8 работников, 400 соединений, 1200 lifecycle-доставок (400 повторов), без потерь; SQLite 4.03 с / p95 цикла 136.94 мс, PostgreSQL 1.09 с / p95 26.11 мс. Это локальный smoke замер, не production SLO и не предел производительности.
- Сквозные проверки реального HBBR и итоговые сборки продолжаются.

### REL-OPS-02 — завершение
- Итог: полный Go test/vet и статическая сборка API прошли с тремя обычными binary-тестами; opt-in PostgreSQL/load сценарии отдельно завершились PASS. Расширенный rotation-тест подтвердил экспорт, архив на диске, повтор, audit намерения/результата. Права проверены для anonymous, relay credential и обычного пользователя; при отказе audit команда не отправляется.
- Настоящий HBBR: 64 TCP-пары / 4 MiB за 1.62 с, затем 60 секунд control outage с непрерывной проверкой установленной пары; restart HBBR/API, доставка очереди и новая передача прошли. Полный сценарий занял 65.29 с. Это ограниченный smoke, не предельная нагрузка.
- Rust fmt/clippy без warnings и 38 unit tests прошли. Добавлен сценарий отказа записи snapshot после успешного backup с восстановлением из прежнего snapshot. Vue typecheck/build прошли, локальные API/HBBR и dev-образ обновлены. Smoke нового образа: UID 65532, read-only root, network none, restart и snapshot v2 с ревизией успешны.
- Сбой первого полного Go-прогона относился к fixture: helper входа из другого теста использовал другой тестовый пароль. Исправлен явный вход fixture; полный повтор прошёл. Vite первоначально блокировался sandbox при чтении родительского каталога конфигурации, разрешённый локальный запуск завершился успешно. Это не дефекты production-регистрации или UI.
- Одноразовые PostgreSQL-контейнер/том/internal network и smoke-контейнер/том/fixtures удалены; рабочие данные не затрагивались. Проверены 55 локальных ссылок документации. Карта, состояние, решения, контракт, testing и relay DEVELOPMENT обновлены.
- REL-OPS-02 завершён в согласованном локальном объёме. Браузерная визуальная проверка не выполнялась; длительный soak, предел одновременных data-пар, ARM64, официальный клиент, multi-API и внешний TLS-стенд остаются открытыми. Архивы бессрочны, restore архива в активную очередь и повторное применение не реализованы. Публикации и production deployment не выполнялись.

## REL-ARM64 — отложена проверка
- По указанию пользователя ARM64 пока не проверяем. Обновлены текущие критерии REL-02/PAR-01, добавлена отложенная цель REL-ARM64, синхронизированы состояние и решение DEC-006. Исторические записи сохранены.
- Изменена только документация; проверки кода/сборки не запускались. Следующий предложенный этап — официальный клиент на amd64; публикации и production по-прежнему запрещены.

## PAR-01 — начало клиентской приёмки amd64
- Пользователь подтвердил следующий этап. Проверить официальные клиенты и отдельный локальный стенд: login/approval/2FA/адресная книга/ACL/relay/revoke, исправлять найденные несовместимости. ARM64 исключена, публикации и production запрещены. Статус карты обновлён до выполнения.

### PAR-01 — официальный контракт 1.4.9
- Проверена SHA256 официального amd64 deb по digest GitHub release; исходники закреплены tag/commit локальным manifest. Установленный Windows-клиент содержит рабочие данные, для приёмки выбран отдельный Linux amd64 GUI-контейнер.
- Обнаружено расхождение TOTP wire protocol: официальный диалог ожидает type=email_check/tfa_type=tfa_check и отправляет type=email_code с tfaCode. API адаптирован, прежний tfa_code сохранён; добавлен camelCase verificationCode. Регрессионный сценарий обновлён по официальным исходникам. Сквозная приёмка ещё не завершена.

- Официальный Linux amd64 клиент вошёл под синтетическим пользователем, открыл личную книгу и после обновления показал запись из API. Windows-профиль не менялся.
- Найдена ещё одна несовместимость контракта: API игнорировал current/pageSize и повторял весь список на каждой странице. Добавлена ограниченная пагинация peers/shared profiles со стабильным порядком и валидацией; вызовы без параметров сохраняют прежнее поведение. Тест проверяет отсутствие повторов, пустую последнюю страницу и ошибочные параметры.

### PAR-01 — частичный результат и передача продолжения

- В официальном Linux amd64 1.4.9 подтверждены вход/выход, личная книга и отображение синтетической записи после обновления. После включения TOTP появился правильный диалог. Успешный TOTP-вход в GUI не подтверждён: noVNC терял пакетные нажатия, затем истекали код/challenge и сработал штатный login limiter. Отказы отображаются в клиенте; лимиты и TOTP не ослаблялись.
- Изменения backend: `art-api/internal/httpapi/server.go`, `address_books.go`; регрессии — `login_integration_test.go`, `address_books_integration_test.go`. Дополнительно проверены ответ на неверный официальный TOTP без токена и страницы shared profiles после фильтрации прав, без скрытых/личных книг и повторов.
- Итоговый полный `go test -count=1 ./...` с `RDS_TEST_HBBR_BINARY` и `go vet ./...` прошёл, httpapi 29.282 с. Три обычных binary-сценария включены; opt-in нагрузка/PostgreSQL в этом блоке не повторялись. Новый тест shared profiles первоначально использовал GET вместо официального POST; fixture исправлен, полный повтор PASS. Go fmt и статическая amd64 сборка API выполнены. Vue/Rust исходники в PAR-01 не менялись, их прежние проверки не объявляются новыми.
- Создание тестового relay и выдача индивидуального enrollment credential отклонены automatic approval review: требуется конкретное одобрение получателя, ресурса и scope. Пользователю отправлен запрос на узел PAR01 и его WSS-канал локального стенда; ответа в этом блоке нет. Узел и ключ не созданы. Ранее отклонённый вариант ручной записи bootstrap-пароля в env заменён штатным runtime-bootstrap API без такого файла.
- Добавлена `official-client-acceptance.md`, обновлены индекс, testing, состояние и карта. Проверены 50 локальных ссылок. PAR-01 остаётся открытой; подробная матрица разделяет GUI и API-тесты.
- Остановлены только тестовые `rds-par01-client-view`, `rds-par01-tls`, `rds-par01-hbbs`, `rds-par01-api`; данные/контейнеры сохранены. Временный файл одноразового кода очищен. Для продолжения запускать API, затем TLS proxy, HBBS и GUI-клиент; runtime находится в `work/par01`, значения секретов не выводить. После lockout повторить успешный TOTP GUI-вход; после разрешения — второй клиент и relay/ACL/revoke. Windows-профиль, ARM64, GitHub/Docker Hub и production не изменялись.

### PAR-01 — разрешён тестовый relay
- Пользователь явно разрешил создать локальный узел PAR01 relay и выдать ключ только для его WSS-канала. Продолжение существующей цели: enrollment, запуск отдельного relay и клиентская приёмка amd64; запреты публикаций/production и отсрочка ARM64 сохраняются.

- Узел создан, enrollment обменян по проверенному TLS, индивидуальный credential сохранён только в runtime. Relay запущен с UID 65532, read-only root, отдельным постоянным томом очереди и без публикации портов на host. API подтвердил control_connected=true, mode=wss, health=healthy; повторное подключение после restart успешно. Relay credential получил 401 на admin users API.
- Исправлен TLS-fixture work/par01/tlsproxy.go: прежний CA использовался как end entity и был отвергнут Rust TLS с CaUsedAsEndEntity. Теперь отдельные CA/server certificate, сохранение сертификатов между restart, явный SSL_CERT_FILE на relay; проверка TLS не отключалась. Производственный код не менялся, Go/Rust/Vue тесты повторно не запускались.
- GUI-клиент при возобновлении завершился с cannot open display; клиентская приёмка и успешный TOTP-вход остаются открыты. Разрешение на enrollment больше не является блокером. API/TLS/HBBS/relay оставлены запущенными только локально; прежний relay-контейнер сохранён остановленным вместе с общим томом. Следующий шаг — восстановление изолированного GUI и второй клиент, затем relay/ACL/revoke. Публикаций и production изменений нет.

### PAR-01 — продолжение клиентских сценариев
- Пользователь подтвердил продолжение: восстановить GUI, проверить TOTP, две официальные сессии через relay, ACL/revoke и клиентскую адресную книгу. Объём остаётся в существующей PAR-01; ARM64 и публикации исключены.

- GUI восстановлен удалением только stale Xvfb lock/socket в одноразовом контейнере. Успешный GUI TOTP-вход официального Linux 1.4.9 подтверждён: после полной вставки кода диалог закрылся и личная книга загрузилась. TOTP/lockout не ослаблялись; прежние неподтверждённые результаты остаются историческими.

- Официальный handshake выявил несовместимость выдачи публичного ключа HBBS: STANDARD_NO_PAD отвергается клиентом 1.4.9 как invalid public key. Тот же ключ с padding прошёл handshake до запроса пароля peer. План в рамках PAR-01: канонический padded Base64 для публичного ключа, нормализация прежнего .pub без ротации приватного ключа, регрессионная проверка и новая amd64 сборка.

### PAR-01 — подтверждённые результаты второго клиентского блока
- Два официальных Linux amd64 клиента 1.4.9 установили защищённое соединение. GUI A показывает рабочий стол B; контрольный ввод независимо виден в GUI B. Включён Always connect via relay; API показал одну активную пару, трафик и healthy WSS. Вторая запись в книге загружена после refresh.
- Исправлен art-core/src/transport.rs: публичный ключ выдаётся как padded Base64; прежний .pub нормализуется при загрузке без ротации приватного файла. Дополнен regression loads_official_padded_rustdesk_key_pair: строгий decoder, нормализация, сохранность приватного файла. Rust fmt, 38 workspace tests и clippy без warnings PASS; release amd64 HBBS собран и установлен только в PAR-01. Сохранность пары и 44-символьный канонический публичный ключ проверены. Прежний HBBS-контейнер сохранён остановленным.
- Rust-проверки выполнены в локальном rustdesk-rust:par01 (Rust 1.98.0 Linux musl, rustfmt/clippy/build tools), с явным CARGO_BUILD_TARGET=x86_64-unknown-linux-musl. Кэш registry подключён отдельно от cargo bin. Первые запуски обнаружили несовпадение host/toolchain/кэша, затем правильное Linux-окружение завершило проверки. Go/Vue в этом блоке не менялись и их тесты не повторялись.
- Файловая сессия не принята: после пароля официальный peer сообщил No active console user logged on. Одноразовый пароль меняется после завершения сессии; устаревший пароль корректно отклонён. Попытка отдельно запустить штатный Connection Manager не создала работающей console/session. Синтетический файл сохранён на A, передачи/SHA-256 PASS нет. Нужен стенд с полноценной Linux desktop session.
- Automatic approval review отклонил scoped ACL deny для тестовой пары: потребовал конкретное одобрение субъекта/устройства/права. Отправлен запрос на ACL deny/restore, revoke тестовых сессий и регистрацию/approval отдельного тестового аккаунта. Дополнительно отклонена подготовка 101 записи вместе с очисткой временного OTP-файла; отдельно запрошено добавление 101 записи в тестовую книгу. Отклонённые команды не выполнены; подтверждений в текущем блоке нет.
- Матрица, состояние и roadmap синхронизированы; PAR-01 остаётся открытой. Production, GitHub/Docker Hub, Windows-профиль и ARM64 не затрагивались.

### PAR-01 — согласованы отрицательные проверки и большая книга
- Пользователь явно разрешил ACL deny/restore для ранее указанной тестовой пары, отзыв тестовых сессий, регистрацию/approval par01.pending и добавление 101 синтетической записи в книгу par01.client. Выполняется в существующей PAR-01; production и публикации исключены.

### PAR-01 — большая книга, ACL и отзыв проверены
- Добавлена согласованная 101 синтетическая запись, две прежние сохранены. API: total 103, страницы 100 + 3, 103 уникальных ID. Официальный GUI после refresh нашёл PAR01 page 101 со второй страницы.
- Scoped ACL deny показал Connection denied: access is not permitted by policy. Отключение единственного правила оставляет default-deny, поэтому для точного восстановления исходного пустого набора удалено только созданное тестовое правило. После этого подключение снова открыло рабочий стол.
- Force-relogin только согласованного пользователя: прежний API-токен отклонён (401), новая GUI-попытка отклонена с session revoked. Уже открытая связь продолжала передавать контрольный ввод; разрыв активной пары не подтверждён, фактический direct/relay маршрут именно этой повторной пары отдельно не зафиксирован. Соединение затем закрыто через GUI.
- Новый синтетический аккаунт зарегистрирован через API с восьмисимвольным паролем: pending. На время одной регистрации включена локальная registration_enabled, затем исходное false восстановлено; auto-approve оставлено false. Вход pending-пользователя в официальный GUI успешен; отрицательная проверка подключения и approval продолжаются.

### PAR-01 — approval и итог текущего блока
- До approval официальный GUI показал ожидание подтверждения администратора. После согласованного admin approve повтор с тем же входом дошёл до пароля peer; повторный login не потребовался. Попытка завершена через Cancel. GUI-форма регистрации отдельно не проверялась: регистрационный запрос выполнялся через API.
- Матрица official-client-acceptance.md, project-state.md и roadmap-status.md обновлены. PAR-01 остаётся частично принятой: принудительный разрыв активной связи и файловая передача не приняты; shared profiles/другие ОС требуют GUI-проверок. Следующий шаг — воспроизвести отзыв при API-подтверждённой relay-паре и проверить доставку disconnect, отдельно определить ограничения direct.
- Код в этом блоке не менялся; повторные сборки, Go/Rust/Vue тесты не выполнялись. Изолированный стенд и синтетические данные сохранены. Production, GitHub/Docker Hub, рабочий Windows-профиль и ARM64 не затрагивались.
- Итоговая проверка: 38 локальных ссылок в индексе и затронутых документах разрешаются; исходный пустой ACL восстановлен, регистрация выключена, approval обязателен, синтетический аккаунт approved.
